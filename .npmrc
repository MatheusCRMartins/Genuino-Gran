# Permite instalar pacotes com peer deps em versões diferentes (necessário pra
# react-three/drei@10 que exige React 19, enquanto o projeto roda React 18).
# Aplica tanto no `npm install` local quanto no `npm ci` do GitHub Actions.
legacy-peer-deps=true
